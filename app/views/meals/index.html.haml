.row
  .span1
    =link_to "New Meal", new_meal_path
  .span11
    %table.table.table-bordered.striped
      %thead
        %tr
          %th Name
          %th Description
      %tbody
        =render(@meals) || raw("<tr><td colspan=2>You have not added any meals.<br/>Add a new one on the left.</td></tr>")

=content_for :javascript do
  :javascript
    $(function() {
      $("table tbody tr").click(function() {
        document.location = "meals/" + $(this).data('meal-id');
        return false;
      });
    });

